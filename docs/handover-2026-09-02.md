# MoneyMoney 交接文档 -- 2026-09-02 终版  
本轮已验证 build test core 在线 127.0.0.1:3000  
  
## 1 摘要  
- 按你要求移除登录门禁 仪表盘直达  
- 修 F12 SyntaxError 白屏乱码 mojibake  
- 隐藏默认账号红条 banner  
- 右侧选择事件侧栏可收起 左侧全屏查看  
  
## 2 架构与关键路径  
- server.ts 4010行 Express api直通 登录已禁用  
- auth.ts HS256 JWT 12h 限频5/60s 黑名单内存 Cookie HttpOnly  
- public/index.html 仪表盘 layout grid 1fr 450px  
- public/login.html 保留但不再强制跳转  
- telegram-bot + command-center 轮询 allowlist  
- release/core/MoneyMoneyCore.exe pkg node24-win-x64 约127MB  
  
## 3 环境变量  
- APP_HOST=127.0.0.1 APP_PORT=3000 LAN_MODE=false  
- 登录凭据从本机受限环境读取，不写入交接文档
- Telegram 轮询配置从受限环境读取；本机代理细节不写入交接文档
- 启动必须 WorkingDirectory=E:/MYC/predict-fun-trader 否则读不到.env 
  
## 4 关键改动时间线  
### 4.2 2b928bb 移除登录门禁  
- server.ts 243 requireAuth 改为 next()直通  
- GET login 移除 verifyToken redirect检查  
- index.html 守卫改为 登录已禁用 mm_isLoggedIn=true  
### 4.3 1fab6ce 乱码白屏+警告条  
- 现象 F12 Missing initializer all.js 19277 常量 已过期  
- 根因 PowerShell Set-Content utf8 导致 mojibake 标识符含点  
- 修复 Node split join isExpired lowLiq 快捷下单  
- 警告条 sec-banner 保留display none 注释掉 fetch isDefault  
### 4.4 495e6eb+20a14d5 侧栏可收起  
- CSS layout sidebar-collapsed grid 1fr 0 transition 0.28s  
- HTML #app-sidebar + sidebar-toggle + collapse-btn + expand-fab  
- JS toggleSidebar localStorage mm_sidebar_collapsed 
  
## 5 测试  
- npm run build tsc copy-web-assets OK  
- npm test 51/51 pass  
- node --check all.js check ok  
- curl api/risk/overview 无Cookie 200  
- 浏览器 Ctrl+F5 后点收起按钮可全屏  
  
## 6 构建发布  
- npm run build  
- npm test  
- Stop-Process MoneyMoneyCore  
- pkg . --targets node24-win-x64 --output release/core/MoneyMoneyCore.exe  
- Start-Process WorkingDirectory E:/MYC/predict-fun-trader  
- 必须先build再pkg 否则旧乱码打进exe 
  
## 7 排错  
- 127.0.0.1拒绝: netstat 3000 WorkingDirectory .env  
- 白屏: node --check + Ctrl+F5  
- 红条: 源码应为注释且sec-banner display none  
- TG无回复: 检查唯一轮询实例、offset 和受限环境配置
- Git push超时: 检查当前网络代理配置，建议使用 HTTPS
  
## 8 Git记录  
- 20a14d5 chore rebuild exe  
- 495e6eb feat sidebar collapsible  
- 1fab6ce fix mojibake banner  
- 2b928bb fix disable login gate  
- 已推送 origin/master = 20a14d5  
  
## 9 待办预留  
- blueicx.ccwu.cc 反代 pm2 Nginx 预案  
- 单用户不做多用户2FA  
  
## 10 Checklist  
- 本机 E:/MYC/predict-fun-trader node>=18 npm install  
- .env 已配置所需服务变量；敏感值不写入交接文档
- build+test全绿  
- pkg重打exe并WorkingDirectory启动  
- 127.0.0.1:3000 Ctrl+F5 验证无红条无白屏侧栏可收起  
- Telegram 私聊交互验证待用户执行
  
生成: 2026-09-02 04:50 终版 Build OK Test 51/51 Core 127.8MB 已推送  

## 11 2026-09-03 AWS US 部署

### 11.1 部署架构与数据路径

- 目标节点：AWS US，公网地址 `100.54.222.54`；本次未修改 AWS 安全组或现有服务端口。
- 系统：Ubuntu 26.04 LTS x86_64；Node `v22.22.1`；npm `9.2.0`。
- 部署目录：`/opt/moneymoney`；运行用户和组：`moneymoney`。
- systemd：`/etc/systemd/system/moneymoney.service`，服务名 `moneymoney.service`，已 enable 且当前 active。
- 实际启动命令：`/usr/bin/node /opt/moneymoney/dist/web/server.js`；不是 `npm start`。
- 环境文件：`/etc/moneymoney/moneymoney.env`，权限 `640 root:moneymoney`。Token 只存在该受限文件，不进入代码、Git、日志或交接文档。
- 网络绑定：`APP_HOST=127.0.0.1`、`APP_PORT=3001`；Telegram 走远端直连，不依赖本机代理。
- 远端数据：SQLite 主文件、WAL、SHM 及 Telegram 状态等 data 文件已从本机实际运行目录迁移；主数据库为 `/opt/moneymoney/data/moneymoney.sqlite`，状态文件位于 `/opt/moneymoney/data/`。
- 运行边界：远端保持 view-only，未迁移 `PRIVATE_KEY`；本机 MoneyMoney 与 MoneyMoneyCore 进程已停止。

### 11.2 启停、重启、状态、日志与健康检查

以下命令均不包含 Token 或其他敏感值：

```bash
sudo systemctl start moneymoney.service
sudo systemctl stop moneymoney.service
sudo systemctl restart moneymoney.service
sudo systemctl is-enabled moneymoney.service
sudo systemctl is-active moneymoney.service
sudo systemctl status moneymoney.service --no-pager
sudo journalctl -u moneymoney.service -n 100 --no-pager
curl -fsS http://127.0.0.1:3001/api/health
curl -fsS http://127.0.0.1:3001/api/telegram/status
```

环境文件变更后必须执行 `sudo systemctl restart moneymoney.service`，再重新检查 active、健康接口和 Telegram 状态接口。

### 11.3 Telegram 轮询冲突与 Token 更换

- 同一 Bot 只能保留一个有效轮询实例；部署前已停止本机 MoneyMoney/MoneyMoneyCore，AWS 节点由 systemd 单实例托管。
- 出现 401/409 或疑似重复轮询时，先确认本机和 AWS 上是否存在旧实例，再只保留一个 `moneymoney.service`，不要同时启动开发服务器、旧桌面核心和 AWS 服务。
- 更换 Token 时，仅通过受限环境文件更新，不写入 service 单元、代码、Git、命令行历史或日志；更新后检查 `640 root:moneymoney` 权限并重启服务。
- 更换后只检查脱敏后的 systemd 状态、健康接口、Telegram 状态接口和日志中的轮询启动结果；禁止在聊天、文档或日志中输出 Token。

### 11.4 备份与回滚

- 保留本机 `release/core` 原有 SQLite 主文件、WAL、SHM、data 状态和原启动入口，禁止删除；它们是回滚基线。
- 远端回滚前先停止 `moneymoney.service`，再按同一数据集恢复 SQLite 主文件、WAL、SHM 和 `data/` 状态文件，确认属主权限后启动服务。
- 回滚后必须依次检查 `systemctl is-active`、`/api/health`、`/api/telegram/status` 和 `journalctl`；不要仅以进程存在或端口监听作为成功依据。
- 本机原始运行数据与桌面启动入口不因 AWS 部署而覆盖或删除。

### 11.5 资源边界与安全边界

- VPS 约有 `908MB RAM`，根盘约 `2.1GB` 可用空间。
- 保持 view-only，不安装 Playwright、Android SDK/NDK 或其他大组件；避免在该节点进行构建和重型分析。
- 服务只绑定 `127.0.0.1:3001`，不新增公网监听；外部访问需另行设计并经确认后实施。

### 11.6 本次实际验证结果

- systemd：`moneymoney.service` 已 enable，当前 active。
- 健康检查：`GET http://127.0.0.1:3001/api/health` 返回 `ok=true`、`storage.ok=true`、`migrationErrors=[]`。
- Telegram 状态：返回 `configured=true`、`pollingEnabled=true`、`pollingRunning=true`、`allowedChatCount=1`。
- `better-sqlite3` 原生模块可正常加载。
- 执行 systemd restart 后，服务仍 active，健康和 Telegram 状态仍正常；journal 出现 `interactive polling started`，未发现 401/409 错误。
- 本地 Linux 兼容性补丁已通过 build；测试 `52/52`；`security:scan` 通过。
- `npm audit` 报告 6 个漏洞（1 low、2 moderate、3 high）；本次未执行 `npm audit fix`，避免改变运行行为。

### 11.7 未完成项

- 仍需用户实际向 Bot 发送一次 `/status`，完成最终真实交互验证。
- 如需恢复交易能力，必须另行设计安全的密钥注入和权限方案；不得将私钥写入交接文档、代码、Git、日志或命令行历史。

## 12 2026-09-03 登录门禁与域名交接补充

本节以本次实际部署状态为准，覆盖前文的旧登录和旧域名预案描述。

- 远端登录门禁已恢复：未登录访问 `/` 返回 `302` 到 `/login?next=%2F`；受保护 API 返回 `401`；健康检查保持公开可用。
- 远端新版本已部署到 `/opt/moneymoney/dist`，`moneymoney.service` 重启后保持 active；旧版本备份位于 `/opt/moneymoney/dist-backup-20260903-205040`。
- 域名固定为 `bluetrade.bbroot.com`，权威 DNS A 记录已确认指向 `100.54.222.54`，无 AAAA 记录。
- Nginx 已安装并启用，配置为 `/etc/nginx/sites-available/moneymoney.conf`，HTTP 反代到 `127.0.0.1:3001`；`nginx -t` 通过。
- 当前尚无该域名证书。Let’s Encrypt 申请因 `bbroot.com` 域名级频率限制被拒绝，官方允许重试时间为 `2026-09-03 13:25:27 UTC`（北京时间 `21:25:27`）。
- 现有 Xray 进程占用公网 `443`，Reality 目标为 `www.apple.com:443`；未停止或修改该 VPN 服务。要启用标准 HTTPS，需要等待证书限制解除并规划 443 端口归属，或改走 DNS-01/独立 HTTPS 入口。
- 未登录门禁和 Nginx 本机反代已完成验证；在 HTTPS 完成前，不应通过公网 HTTP 提交登录密码。

## 13 2026-09-03 443 端口协调结果

- 经 VPN 负责人 VSI 评估并获用户授权，Xray Reality 已从公网 `443` 迁移到 `8443`；`xr.service` 当前 enabled/active，配置校验通过。
- Xray 原配置和服务单元已保留受限回滚备份；备份不包含在项目或本交接文档中。
- 本机当前 Clash、sing-box 和节点清单中的 AWS US 节点端口已同步改为 `8443`，其它 VPN 参数保持不变；历史备份文件未修改。
- 远端当前监听：Nginx `80`、Xray `8443`、MoneyMoney `127.0.0.1:3001`；公网 `443` 已释放给网站接管，暂未配置证书。
- AWS 安全组已放行 TCP `8443`，并已完成公网 TCP 连通性验证；来源范围仍应按用户安全策略维护。
- AWS 安全组现已放行 TCP `80`，公网 TCP 80 已验证可达；域名 HTTP 请求返回登录门禁 `302`。
- Let’s Encrypt 仍受 `bbroot.com` 域名级频率限制，最新允许重试时间为 `2026-09-03 20:10:38 UTC`（北京时间 `2026-09-04 04:10:38`）。在证书签发前，不要通过公网 HTTP 提交登录密码。

## 14 2026-09-04 Telegram 底部菜单分页

- 底部功能键盘改为 3 页；每页最多 4 行功能按钮，底部增加 `⬅ 上一页`、`菜单 X/3`、`下一页 ➡` 导航。
- 页码按 Telegram chatId 在进程内隔离，重启服务后各聊天从第 1 页开始；不写入数据库、Token 或交接敏感配置。
- 导航只切换菜单页，不触发业务动作；现有文字菜单、斜杠命令、内联回调和异步通知保持原有处理路径。
- 本地验证：`npm run build` 通过，`npm test` 57/57，通过 `security:scan`（205 个受跟踪文件）。
- 部署时仅替换远端 `/opt/moneymoney/dist`，保留带时间戳旧版本备份；回滚方式为停止服务、恢复旧 `dist`、确认属主后启动并复查 `/api/health`、`/api/telegram/status` 和 journal。
- 本次实际部署：远端新版本校验为 `8f689bb1b3b0be5d065797413c5cdf684e01a1c798c879b1cc03dc7fcafe06bb`；旧版本备份为 `/opt/moneymoney/dist-backup-20260904-telegram-menu`。
- 部署后验证：`moneymoney.service` active，健康接口返回 `ok=true`、`storage.ok=true`；Telegram 轮询日志显示 `interactive polling started (1 allowed chat)`；未修改环境文件、Nginx 或 Xray。

## 15 2026-09-04 Telegram 指令表与股票搜索

- `/help` 现在按“市场与风险 / 自选与模拟盘 / 研究、提醒与自动化 / 工具与诊断”分组展示快捷指令；这些内容只在快捷指令表中显示，不增加底部键盘行数。
- `/search <关键词>` 同时查询预测市场快照和腾讯股票搜索结果；股票结果提供“加入自选”和“行情”按钮，预测市场结果继续提供“加自选 / 解释 / 开仓”。
- 股票自选 ID 使用统一市场代码（例如 `usAAPL`）；`/watchlist` 会按股票显示代码，`/watch add usAAPL` 也可直接加入。
- 本次本地验证：`npm run build`、`npm test` 60/60、`security:scan` 和 `git diff --check` 均通过。
- 本次 VPS 部署：旧版本备份为 `/opt/moneymoney/dist-backup-20260904-search-actions`；远端 `server.js` 与 `telegram-search.js` 校验通过，`moneymoney.service` active，轮询日志显示 1 个允许聊天。

## 16 2026-09-04 证书申请结果

- 已对 `bluetrade.bbroot.com` 执行一次 Certbot Nginx 申请；DNS、HTTP 80 验证入口和 Nginx 配置均正常。
- Let’s Encrypt 返回域名级频率限制：`bbroot.com` 过去 168 小时已签发 50 张证书；下一次允许重试时间为 `2026-09-04 09:35:07 UTC`（北京时间 `17:35:07`）。
- 本次申请未生成证书，Nginx 配置未切换到 HTTPS；MoneyMoney 与 Xray 服务仍 active，监听边界未改变。到允许时间后只需重新执行同一 Certbot 申请。

## 17 2026-09-04 ZeroSSL HTTPS 部署

- Let’s Encrypt 频率限制期间改用 ZeroSSL ACME；`bluetrade.bbroot.com` HTTP-01 验证通过，证书已签发，有效期为 `2026-09-04` 至 `2026-12-03`。
- 证书由 `acme.sh` 管理，证书文件安装在 `/etc/ssl/moneymoney/fullchain.pem`，私钥为 `/etc/ssl/moneymoney/privkey.pem`，私钥权限为 `root:root 600`。
- Nginx 已启用公网 `80/443`：HTTP 请求 301 跳转 HTTPS，HTTPS 反代到 `127.0.0.1:3001`，登录门禁仍返回 `/login?next=%2F`；配置备份为 `/etc/nginx/sites-available/moneymoney.conf.bak-20260904-zerossl`。
- `acme.sh` 自动续期任务已安装，续期后自动 reload Nginx；当前登记的下一次续期时间为 `2026-11-19 06:55:46 UTC`。
- Xray/VPN 未修改，`xr.service` 保持 active，公网 `8443` 监听保持不变；`moneymoney.service` 和 Nginx 也均为 active。
- 实际验收：`nginx -t` 通过；HTTP 返回 301；HTTPS TLS 成功并返回登录门禁 302；证书主体为 `bluetrade.bbroot.com`。

## 18 2026-09-04 高影响事件提醒与结果通知

- 高影响事件提醒节点固定为提前 `24 小时 / 12 小时 / 6 小时 / 3 小时 / 1 小时 / 30 分钟 / 10 分钟 / 5 分钟`；同一聊天、事件和节点只发送一次。
- 事件提醒改为独立 60 秒监控；服务启动时只建立当前阶段基线，不补发已经错过的节点；单次轮询跨过多个节点时只发送最新节点。
- 事件公布实际值后发送一次结果通知，包含实际值、预期值、超预期方向和“偏利好 / 偏利空 / 方向不明”。判断采用指标关键词的保守映射，无法可靠判断时不强行标注。
- 结果通知只在实际值从未公布变为已公布时触发；服务重启时已有实际值只建立基线，不重复通知；暂停和免打扰期间不标记发送，恢复后继续尝试。
- 本地验证：`npm run build` 通过，`npm test` 61/61，`git diff --check` 通过。
- 本次 VPS 部署：新版本备份为 `/opt/moneymoney/dist-backup-20260904-event-alerts-status`；旧事件提醒版本备份为 `/opt/moneymoney/dist-backup-20260904-event-alerts`。
 - 部署后验证：`moneymoney.service` active，健康接口 `ok=true`，Telegram `pollingRunning=true`，命令中心监控状态显示 `price=true`、`event=true`、`slow=true`；Nginx HTTPS 和 Xray `8443` 均保持正常。

## 19 2026-09-04 网页凭据配置

- 设置页新增 AI OpenRouter/Groq API Key 和 Telegram Bot Token、Chat ID、允许/管理员 Chat ID、代理 URL、轮询开关输入。
- 网页提交到登录保护的 `/api/settings/secrets`；密钥不进入普通 `settings.json`，不在 API 响应或页面中回显，保存到 `/opt/moneymoney/data/runtime-secrets.json`（运行用户可读）。
- 网页保存 Telegram 配置后会自动重载当前进程内的机器人和提醒监控，不需要手动重启；空白密钥字段保持原值。`.env` 仍可作为未设置网页凭据时的回退来源。
- 本地验证：`npm run build` 通过，`npm test` 63/63。
- 本次 VPS 部署：旧版本备份为 `/opt/moneymoney/dist-backup-20260904-web-secrets`；部署后 `moneymoney.service`、Nginx、Xray 均 active，域名 HTTPS 返回登录门禁 `302`。

## 20 2026-09-08 市场优先导航发布

- 顶部市场栏统一为：总体、股票、期权、虚拟币、预测市场、自选；搜索、总设置、自动化运营紧随自选。
- 通用工具栏包含证据工作台、新闻日历、回测、策略对比、提醒、巨鲸监控；底部核心导航使用跨市场的持仓入口，不再重复显示币安。
- 搜索请求携带当前市场范围，市场切换会同步 URL、本地状态并丢弃旧范围的异步结果，避免不同市场数据混淆。
- 本地验证：`npm run build`、`npm test`（91/91）、`node scripts/secret-scan.cjs`、`git diff --check` 均通过；浏览器实测切换虚拟币范围后搜索 AAPL 返回无匹配。
- GitHub 已推送到 `origin/master`，提交 `dcac7b8`；本次交接记录随后追加的提交另行记录在 Git 历史中。
- VPS 发布：旧版本备份为 `/opt/moneymoney/dist-backup-20260908-081111-market-nav`；`moneymoney.service` active，`/api/health/live` 返回 `ok=true`，HTTPS 登录页返回 200。
- 远端构建产物与本地 `dist/web/server.js`、`dist/web/public/index.html` SHA-256 一致；Nginx、证书和 VPN 未修改。

## 21 2026-09-08 市场作用域隔离发布

- 分析、风险、风险历史、校准、导出和顶部持续信息均按总体/股票/期权/虚拟币/预测市场/自选作用域隔离；宏观工作区归入通用工具栏。
- 非预测市场不再显示旧预测盘的雷达、VaR、恐贪/资金费率/CFTC 等跨市场内容；持续信息接口对股票和期权无跨市场回退。
- 本地验证：`npm run build` 通过，`npm test` 106/106，专项作用域测试 15/15，`security:scan` 和 `git diff --check` 通过。
- GitHub `origin/master` 已推送至提交 `b08f8dbab491c2c157cff239c067c1e87e5a9b3b`。
- VPS 发布后 `moneymoney.service` active，监听 `127.0.0.1:3001`，健康接口和 HTTPS 登录页正常；远端 `server.js` SHA-256 与本地一致。
- 本次有效旧版本备份为 `/opt/moneymoney/dist-backup-20260908-181211`；部署过程中产生的失败快照 `/opt/moneymoney/dist-failed-20260908-1818` 保留用于回溯；Nginx、证书和 VPN 未修改。

## 22 2026-09-08 市场雷达入口隔离修复发布

- 修复上次标签恢复和底部“雷达”入口固定打开预测雷达的问题：股票显示并打开“股票雷达”，期权显示并打开“期权雷达”，虚拟币显示并打开“币安雷达”；预测雷达仅在总体/预测市场作用域可见。
- 本地验证：`npm run build` 通过，`npm test` 107/107，`security:scan` 和 `git diff --check` 通过；线上切换股票/期权并点击对应雷达入口均保持在对应工作区。
- GitHub 修复提交：`2bcb13e`；VPS 发布后 `moneymoney.service` active，`127.0.0.1:3001` 正常监听，健康接口和 HTTPS 登录页正常，远端 `server.js` SHA-256 与本地一致。
- 本次旧版本备份为 `/opt/moneymoney/dist-backup-20260908-2bcb13e`；Nginx、证书和 VPN 未修改。

## 23 2026-09-09 AI 市场作用域提示发布

- AI 市场点评接口现在接收当前 `scope`；股票、期权、虚拟币和自选作用域只传入对应行动/指标，预测市场和总体作用域才读取预测雷达。
- AI 点评缓存签名包含市场作用域和上下文；不同市场的并发请求不会复用另一市场的进行中结果。
- 前端 AI 点评请求同步携带当前市场作用域；旧版不带 `scope` 的调用继续按预测市场兼容处理。
- 本地验证：`npm run build`、`npm test`（123/123）、`npm run security:scan` 和 `git diff --check` 均通过。
- GitHub 分支 `codex/stock-free-data-sources` 已推送至提交 `ef36b87`。
- VPS 发布：旧版本备份为 `/opt/moneymoney/dist-backup-20260909-083302-ai-scope`；`moneymoney.service` active，健康接口正常，HTTPS 登录页返回 200。
- 远端 `server.js` 与 `index.html` SHA-256 和本地构建产物一致；Nginx、证书和 VPN 未修改。

## 24 2026-09-12 独立工作区扁平内容发布

- 选中内部人、机构持仓、分析师共识、基本面质量、空头利息、市场宽度和其他工作区后，中间区只保留当前功能内容；隐藏其他工作区，不再显示当前功能的外层折叠边框、背景和箭头。
- 当前功能的内部内容卡保留；左侧功能导航仍可折叠。扁平工作区即使曾被旧版本地折叠状态写成关闭，也会自动保持展开。
- 实现提交：`199cde0`；本地折叠状态回归修复提交：`d3ddf7a`。GitHub 分支 `codex/stock-free-data-sources` 已推送至 `d3ddf7a`。
- 本地验证：`npm run build`、`npm test`（245/245）、`npm run smoke:web`、`npm run security:scan` 和 `git diff --check` 均通过；安全扫描检查 313 个已跟踪文件。
- 线上浏览器验收：总览页 `flatCount=0`；分析师页 `flatCount=1`、`open=true`、`display=block`、`border=0px`、箭头 `display=none`，内部内容高度 `80px`，确认内容真实渲染而非空壳。
- VPS 发布：版本备份为 `/opt/moneymoney/backups/dist-d3ddf7a-20260912-1045`，回滚目录为 `/opt/moneymoney/dist.rollback-d3ddf7a-20260912-1045`；`moneymoney.service` active，健康接口返回 `ok=true`。
- 远端 `server.js` SHA-256 为 `f4e72cb92018f7f4b2310d384b784c53374fa8323185c24e721bc2b53c20aed2`，`index.html` SHA-256 为 `ace13222d45023c347c637d283085a9292a2846b9c6c05316be781ed7dd91dd1`，均与本地构建产物一致；Nginx、证书和 VPN 未修改。

## 25 2026-09-12 股票总览重复标的与左侧菜单回归修复

- 根因：股票总览仍把 `market-research-tools` 按股票作用域渲染到中间区，筛选接口返回的七姐妹因此再次形成中间卡片；同时中间工作区筛选误作用于左侧 `.workspace-item`，导致左侧只剩分组标题或单个入口。
- 修复：市场筛选工具只保留在“总体”作用域；工作区内容严格按当前工作区显示；左侧导航项从中间内容筛选边界中排除。股票总览中间不再重复七姐妹，七姐妹只保留在右侧标的库。
- 修复提交：`456a2a9`、`57f3739`；GitHub 分支 `codex/stock-free-data-sources` 已推送至 `57f3739`，PR 会自动包含这两次修复。
- 本地验证：`npm run build`、`npm test`（245/245）、`npm run smoke:web`、`npm run security:scan` 和 `git diff --check` 均通过；安全扫描检查 313 个已跟踪文件。
- 线上浏览器验收：股票总览 `centerStockRows=0`、`researchHidden=true`、左侧可见功能项 `14` 个、右侧七姐妹 `7` 个；分析师工作区左侧仍可见 `14` 个，中心可见工作区仅 `analyst`，扁平面板 `open=true`、`border=0px`、箭头隐藏。
- VPS 发布：归档 SHA-256 为 `9246171081c377ce6f08c6aa0ee6e50714640e4f6f56788f21027fb73ecef00f`；版本备份为 `/opt/moneymoney/backups/dist-57f3739-20260912-193520`，回滚目录为 `/opt/moneymoney/dist.rollback-57f3739-20260912-193520`；`moneymoney.service` active，健康接口返回 `ok=true`。
- 远端 `server.js` SHA-256 为 `f4e72cb92018f7f4b2310d384b784c53374fa8323185c24e721bc2b53c20aed2`，`index.html` SHA-256 为 `69d6736f050ba947cbb59609e74f4ecc5294a53b60e784a75e47be2471d8025a`，与本地构建产物一致；Nginx、证书、VPN、密钥和 Telegram 配置未修改。

## 26 2026-09-12 移除左侧重复自选与持仓入口

- 按产品要求移除左侧“组合”分组及其中的“我的自选”“我的持仓”两个重复入口；未删除自选/模拟持仓数据，也未删除右侧标的库、顶部“自选”或底部“持仓”入口。
- 直接访问 `watchlist` / `positions` 工作区仍被允许，避免只清理左侧导航时破坏已有页面和链接；导航测试同时锁定“左侧不显示、工作区仍可访问”的边界。
- 实现提交：`68a8271`；GitHub 分支 `codex/stock-free-data-sources` 已推送，PR #1 会自动包含本次修复。
- 本地验证：`npm run build`、`npm test`（245/245）、`npm run smoke:web`、`npm run security:scan` 和 `git diff --check` 均通过；安全扫描检查 313 个已跟踪文件。
- 线上浏览器验收：股票总览左侧可见功能项 `12` 个，未出现“组合/我的自选/我的持仓”，中间股票重复卡片为 `0`；分析师工作区左侧同样为 `12` 个，中心只显示当前分析师工作区。
- VPS 发布：归档 SHA-256 为 `eb05699d9dcfe29f5300da91fc00c1fd972933c7d7ec149df0e2e63b2ebb4944`；版本备份为 `/opt/moneymoney/backups/dist-68a8271-20260912-200026`，回滚目录为 `/opt/moneymoney/dist.rollback-68a8271-20260912-200026`；`moneymoney.service` active，健康接口返回 `ok=true`。
- 远端 `server.js` SHA-256 为 `f4e72cb92018f7f4b2310d384b784c53374fa8323185c24e721bc2b53c20aed2`，`index.html` SHA-256 为 `840eff076fc33bd20740bd286bc3223c19898e4fac54b14fda61eb08179c396f`，与本地构建产物一致；Nginx、证书、VPN、密钥和 Telegram 配置未修改。

## 27 2026-09-12 功能卡片外框与刷新位置优化发布

- 选中具体功能后隐藏该功能的外层标题/折叠框，只保留内部数据卡片；刷新按钮不再单独占用内容上方一行，而是放进数据卡片标题行。
- 状态徽章保持右侧原位置不动；刷新按钮统一紧贴在状态徽章左侧，内部人、机构持仓、分析师共识、基本面质量、空头利息、市场宽度和全球宏观现货均采用同一布局。
- 实现提交：`65f2473`（包含此前 `72b1fa6` 的外框扁平样式）；GitHub 分支 `codex/stock-free-data-sources` 已推送，PR #1 会自动包含本次修复。
- 本地验证：`npm run build`、`npm test`（245/245）、`npm run smoke:web`、`npm run security:scan` 和 `git diff --check` 均通过；安全扫描检查 313 个已跟踪文件。
- 线上浏览器验收：分析师工作区外层 `summary` 的计算样式为 `display:none`、面板边框为 `0px`；数据卡片中刷新按钮坐标为 `993.2–1080.3`，状态徽章为 `1098.3–1195.3`，确认刷新按钮位于徽章正左侧；左侧功能项保持 `12` 个。
- VPS 发布：归档 SHA-256 为 `477bd96f0558518f6477a78a522d3a31de04ee147dac186c58319410b4ee39bc`；版本备份为 `/opt/moneymoney/backups/dist-65f2473-20260912-202002`，回滚目录为 `/opt/moneymoney/dist.rollback-65f2473-20260912-202002`；`moneymoney.service` active，健康接口返回 `ok=true`。
- 远端 `server.js` SHA-256 为 `f4e72cb92018f7f4b2310d384b784c53374fa8323185c24e721bc2b53c20aed2`，`index.html` SHA-256 为 `babffdf16a728305507bc5e091774d320df88d2746395c5ae3a98ba18ca9cabc`，与本地构建产物一致；Nginx、证书、VPN、密钥和 Telegram 配置未修改。

## 28 2026-09-12 功能卡片刷新键垂直对齐修复发布

- 修复刷新键与状态徽章视觉上不在同一水平线的问题：功能卡片标题行统一使用垂直居中；状态徽章仍保持右侧，刷新键仍紧贴在其左侧。
- 实现提交：`b4a7ae5`；GitHub 分支 `codex/stock-free-data-sources` 已推送，PR #1 会自动包含本次修复。
- 本地验证：`npm run build`、`npm test`（245/245）、`npm run smoke:web`、`npm run security:scan` 和 `git diff --check` 均通过；安全扫描检查 313 个已跟踪文件。
- 线上浏览器验收：分析师工作区标题行 `align-items=center`；刷新键纵向中心与状态徽章一致（均为 `289.8px`），且刷新键横向位于徽章左侧；外层 `summary` 继续为 `display:none`。
- VPS 发布：归档 SHA-256 为 `7de60d0dc9e4d0ce245bdfd5fe310d8ddca4152d9363447eea9b544e6b0417e7`；版本备份为 `/opt/moneymoney/backups/dist-b4a7ae5-20260912-202802`，回滚目录为 `/opt/moneymoney/dist.rollback-b4a7ae5-20260912-202802`；`moneymoney.service` active，健康接口返回 `ok=true`。
- 远端 `server.js` SHA-256 为 `f4e72cb92018f7f4b2310d384b784c53374fa8323185c24e721bc2b53c20aed2`，`index.html` SHA-256 为 `399b29cba323d810a249452990175a8fb073d4c0ecd9ef1db90d0226f1c9fbf2`，与本地构建产物一致；Nginx、证书、VPN、密钥和 Telegram 配置未修改。

## 29 2026-09-12 分析师共识与基本面质量研究扩展发布

- 分析师共识工作区新增目标价低/中位/高区间与现价位置、共识评分历史趋势、最近分析师动态列表；每条动态显示分析师姓名、机构、日期、动作、评级、目标价变化和历史排名，并提供 StockAnalysis 来源链接。
- 当公开 payload 没有可验证观点原文时，界面显示结构化事实摘要，不伪造“某分析师表示/认为”的引语。修复了当前 StockAnalysis payload 的 `}]},ratings:[` 闭合标记和 `-.8` 前导小数解析，线上 AAPL 已实际显示 Tom Forte、Maxim Group 等 5 条动态。
- 基本面质量工作区新增净利率与经营现金流率年度趋势、支持因素、风险因素和缺失字段状态；数据继续来自 SEC EDGAR XBRL，评分、信号和市场作用域边界保持不变。
- 实现提交：`7586832`、`aead0f6`、`4855a4c`、`0f1afb1`、`faf3d9f`；GitHub 分支 `codex/stock-free-data-sources` 已推送至 `faf3d9f`，PR #1 会自动包含本次提交。
- 本地验证：`npm run build`、`npm test`（254/254）、`npm run smoke:web`、`npm run security:scan` 和 `git diff --check` 均通过；安全扫描检查 317 个已跟踪文件。
- 线上浏览器验收：分析师工作区 `actionCount=5`，显示姓名/机构/动作/目标价/历史排名/来源链接；目标价区间和 13 个周期趋势正常。基本面工作区支持因素、风险因素和两条趋势正常；两个工作区外层 `summary` 均为 `display:none`，刷新键位于状态徽章左侧且 `sameVerticalCenter=true`。
- VPS 发布：归档 SHA-256 为 `2699dfb7b3957ab864a7d3ff86eebd706b329e53f1916c43bc22507560185179`；版本备份为 `/opt/moneymoney/backups/dist-faf3d9f-20260912-130518-221717`，回滚目录为 `/opt/moneymoney/dist.rollback-faf3d9f-20260912-130518-221717`；`moneymoney.service` active，健康接口返回 `ok=true`。
- 远端 `features/analyst-consensus.js` SHA-256 为 `7acd4b9a0fe8e6991c0bb56e0a0de42865939fdc00a08898278313882da706c6`，`web/public/index.html` SHA-256 为 `c3d2981238283a35cf472ce12c095f7eb22f0f9942462e9350beedcf46631fd0`，均与本地构建产物一致；仅替换应用 `dist`，Nginx、证书、VPN、密钥和 Telegram 配置未修改。

## 30 2026-09-12 删除重复搜索与主题切换入口发布

- 左侧市场功能区移除“搜索标的”，搜索统一保留在顶部全局搜索；后端 `/api/workspace/navigation` 与前端 fallback 同步移除，股票、期权和虚拟币市场的导航保持一致。
- 删除内容区右上角固定的重复主题按钮 `theme-toggle-btn`；保留顶部刷新键左侧的 `theme-toggle`，主题图标同步逻辑只更新这一处。
- 实现提交：`2991ee5`、`01844a0`；GitHub 分支 `codex/stock-free-data-sources` 已推送，PR #1 会自动包含本次提交。
- 本地验证：TDD 回归测试先红后绿；`npm run build`、`npm test`（256/256）、`npm run smoke:web`、`npm run security:scan` 和 `git diff --check` 均通过；安全扫描检查 317 个已跟踪文件。
- 线上浏览器验收：股票、期权、虚拟币均为 `sidebarSearch=false`、`duplicateTheme=false`、`themeButtons=["theme-toggle"]`、`globalSearch=true`；远端 `moneymoney.service` 为 `active`，live 接口返回 `ok=true`。
- VPS 发布：版本备份为 `/opt/moneymoney/backups/dist-01844a0-20260912-215136`，回滚目录为 `/opt/moneymoney/dist.rollback-01844a0-20260912-215136`；远端 `web/public/index.html` SHA-256 为 `818ee9de46b1b2e7dc8598226f02d4bd2ea02d472e7a425bbfbf986676b95baa`，`web/server.js` SHA-256 为 `f4e72cb92018f7f4b2310d384b784c53374fa8323185c24e721bc2b53c20aed2`，与本地构建产物一致。仅替换应用 `dist`，Nginx、证书、VPN、密钥和 Telegram 配置未修改。
