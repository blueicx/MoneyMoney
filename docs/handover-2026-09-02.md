# MoneyMoney 交接文档 — 2026-09-02

> 本轮：登录加固 + Telegram 填表/中文化/列表化 已完成并验证（npm run build / npm test 51/51）。本机轮询态，不接公网域，不增依赖。

## 1. 摘要
- 目标：给 Web 增加单用户登录页，并让 TG 手机端更易用（开仓填表、搜索/信号列表、事件中文）
- 形态：本机 APP_HOST=127.0.0.1:3000 + TELEGRAM_POLLING_ENABLED=true + allowlist 1927281962 + proxy 127.0.0.1:7897
- 交付：新增 src/web/auth.ts、login.html、tests/auth-hardening.test.cjs；重构 server.ts(~4010行)、config.ts、.env.example

## 2. 架构与关键文件
predict-fun-trader/src/web/server.ts ~4010行, auth.ts, config.ts, telegram-bot.ts, telegram-command-center.ts
详见 git 历史，auth.ts 提供 JWT/限频/blacklist，server.ts 扩展 TG 回调前缀 paper:pick/side/do watch:add/remove explain: search:q:

## 3. 环境变量 见 .env.example
MONEYMONEY_LOGIN_USER=admin, PASS, JWT_SECRET, EXPIRY_MS=43200000, TELEGRAM_PROXY_URL=http://127.0.0.1:7897

## 4. 演示
网页: http://127.0.0.1:3000/login 登录->仪表盘->刷新续期->退出二次确认->再访跳登录
TG私聊1927281962: /start /status /search /search BTC /signals /explain /watchlist /paper -> 选市场 -> 选YES/NO -> 选金额 -> /confirm <nonce> /events中文

## 5. 测试 npm run build + npm test 51/51 pass

## 6. 边界 本机127.0.0.1，不做pm2/域名穿透，预留 pm2 start dist/web/server.js --name moneymoney

## 7. 排错
- 127.0.0.1拒绝连接: 确认 npm run web 在跑
- 登录429: 等 Retry-After
- TG无回复: 查 BOT_TOKEN/ALLOWED_CHAT_IDS/POLLING_ENABLED/proxy

---
生成: 2026-09-02