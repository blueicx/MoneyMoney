# MoneyMoney 交接文档 — 2026-09-02

> 本轮：**登录加固 + Telegram 填表/中文化/列表化** 已完成并验证（npm run build / npm test 51/51）。本机轮询态，不接公网域，不增依赖。

## 1. 摘要
- 目标：给 Web 增加单用户登录页，并让 TG 手机端更易用（开仓填表、搜索/信号列表、事件中文）
- 形态：本机 APP_HOST=127.0.0.1:3000 + TELEGRAM_POLLING_ENABLED=true + allowlist 1927281962 + proxy 127.0.0.1:7897
- 交付：新增 src/web/auth.ts、login.html、tests/auth-hardening.test.cjs；重构 server.ts(~4010行)、config.ts、.env.example

## 2. 架构
- src/web/auth.ts: JWT HS256、blacklist 12h、限频 5/60s 与 10/min、extractAuthToken/requireAuth/buildAuthCookie
- src/web/server.ts: 鉴权守卫 + /login + /api/auth/* + TG回调 paper:pick/side/do watch:add/remove explain: search:q:
- src/config.ts: LOGIN_USER/PASS/JWT_SECRET/EXPIRY_MS，空时随机生成+warn
- telegram-bot.ts: 25s轮询 data/telegram-bot-state.json 去重
- telegram-command-center.ts: version:1->2 pending(6位5min)

## 3. 环境变量
MONEYMONEY_LOGIN_USER=admin / PASS / JWT_SECRET / EXPIRY_MS=43200000 等见 .env.example

## 4. 演示
网页: http://127.0.0.1:3000/login 登录->仪表盘->刷新续期->退出二次确认->再访跳登录
TG私聊1927281962: /start /status /search /search BTC /signals /explain /watchlist /paper -> 选市场 -> 选YES/NO -> 选金额 -> /confirm <nonce> /events中文

## 5. 测试
npm run build + npm test 51/51 pass (2026-09-02)

## 6. 边界
本机127.0.0.1，不做pm2/域名穿透，预留 pm2 start dist/web/server.js --name moneymoney；单账号内存限频重启清零零依赖

---
生成: 2026-09-02
